<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <nav>
      <a href="/">home</a>
      <a href="/about">about</a>
    </nav>
    <h1><%= title %></h1>
    <!-- 아래 name 이 홍길동이면 if문이 실행되고 아니면 else가 실행된다. -->
    <% if(name == '둘리') { %>
    <h1><%= name %>의 홈페이지에 오신것을 환영합니다.</h1>
    <% } else { %>
    <p>아무개님 누구세요?</p>
    <% } %>

    <!-- 값이 표시되지 않음 (처리만 됨) -->
    <% 1 + 1 %>

    <!-- 값이 표시할 때 (%=) -->
    <p><%= 5 * 2 %></p>

    <!-- 반복 -->
    <% for(let i = 0; i < 5; i++) { %>
    <p>출력된 html <%= i %></p>
    <% } %>
  </body>
</html>
